import type { Metadata } from 'next';
import { Action, CTASection, Hero } from '@/components/site/primitives';
import { packages } from '@/content/site';
export const metadata:Metadata={title:'Our Packages',description:'Five flexible communications packages for development programs, businesses, enterprises and communications teams.'};
export default function Packages(){return <><Hero eyebrow="WAYS TO WORK TOGETHER" title="Your context. Your ambition. Our collective thinking." text="A focused starting point for the work you need. Each package brings strategy and execution together, with room to adapt."/><div className="wrap">{packages.map((p,i)=><section id={`package-${i}`} className="package-detail" key={p.name}><div><span className="eyebrow">0{i+1} / THE RIGHT SUPPORT</span><h2>{p.name}<em>Package</em></h2><p>{p.audience}.</p><Action>Start a Conversation</Action></div><div className="deliverables"><span className="eyebrow">WHAT’S INCLUDED</span><ul>{p.items.map(d=><li key={d}>{d}</li>)}</ul></div></section>)}<div className="tailored"><h2>Not every challenge<br/>fits a package.</h2><p>Packages can be tailored to your context. Tell us where you are, where you want to go and what stands in the way.</p><Action>Let’s shape it together</Action></div></div><CTASection/></>}
